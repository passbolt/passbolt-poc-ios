enum AccessibilityIdentifiers {
    enum NavigationBar {
        static let sceneNavigationBar = "scene_navigation_bar"
    }
    
    enum MainView {
        static let milestoneTableCell = "milestone_table_cell_"
    }
    
    enum EncryptionLibrary {
        static let encryptionFeatureTableCell = "encryption_library_table_cell_"
    }
    
    enum EncryptDecrypt {
        static let messageTextView = "message_text_view"
        static let keyTextView = "key_text_view"
    }
}
